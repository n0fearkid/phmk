services:
  - type: web
    name: vk-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: GROUP_TOKEN
        sync: false
      - key: GROUP_ID
        sync: false
      - key: PORT
        value: "10000"